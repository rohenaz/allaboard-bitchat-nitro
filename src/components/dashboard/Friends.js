import { Nav } from "rsuite";
import NavItem from "rsuite/esm/Nav/NavItem";

const Friends = () => {
  return (
    <div>
      <Nav>
        <NavItem
          active={false}
          icon={null}
          href={``}
          onSelect={() => {}}
          disabled={false}
        />
      </Nav>
    </div>
  );
};

export default Friends;
